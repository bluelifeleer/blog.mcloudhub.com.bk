{
  "name": "blog",
  "version": "1.0.0",
  "description": "# express(框架) # swig(模板) # mongodb(数据库) # node(服务器) # Vue(js库)",
  "main": ".eslintrc.js",
  "dependencies": {
    "body-parser": "^1.18.2"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "bluelife",
  "license": "ISC"
}
